



{% block body %}

    <div class="container">
        <div class="container grid-img" id="tricksimages">
        <div class="row justify-content-center">
        
        {% for trick in tricks %}
        
            
            
            
        
                <div class="col-md-4 text-center">
                     <a href="{{ path('trick_show', {'id': trick.id}) }}"><img class="img-fluid img-thumbnail" src="{{asset('upload/' ~ trick.imageList[0].filename)}}"></a>
                    <div class="col-container">
                        <div class="col">
                        <h5> {{ trick.name }}</h5>
                        </div>
                    <div class="col">

                        <a href="{{ path('trick_edit', {'id': trick.id}) }}">
                            <i class="far fa-edit fa-lg"></i> Modifier
                        </a>
                        </div>
                        <div class="col">
                        {{ include('trick/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            
             {% endfor %}
            </div>      
           
    </div>

        
                
           
        
    
   
    

 
</div>
{% endblock %}
